# Copie este arquivo para .env.compose na raiz (docker compose exige o arquivo).
# DATABASE_URL, REDIS_URL, PORT e JWT_SECRET ja sao injetados pelo docker-compose.yml.

FRONTEND_URL=http://localhost:3006
NOTAAS_API_BASE=https://platform.notaas.com.br/api/v1
NOTAAS_AMBIENTE=homologacao

# Opcional — assinatura (Stripe)
# STRIPE_SECRET_KEY=
# STRIPE_PRICE_PRO=
# STRIPE_PRICE_ENTERPRISE=
# STRIPE_WEBHOOK_SECRET=
